# Трекер активности программиста (PostgreSQL + C++)

**Лабораторная работа №6** по дисциплине "Базы данных"

Проект реализует систему учёта времени программиста (по типу WakaTime): трекинг проектов, языков программирования, редакторов и дневной активности. Интеграция PostgreSQL с C++ приложением через libpq.

---

## 📋 Содержание проекта

### Структура файлов
```
.
├── sql/
│   └── init.sql              # SQL-схема БД и тестовые данные
├── src/
│   ├── db_manager.h          # Заголовочный файл менеджера БД
│   ├── db_manager.cpp        # Реализация запросов к БД
│   └── main.cpp              # Интерактивное меню
├── CMakeLists.txt            # Конфигурация сборки
├── docker-compose.yml        # (опционально) Docker для PostgreSQL + Adminer
└── README.md                 # Этот файл
```

### Схема базы данных (6 таблиц)

**1. developers** — разработчики
- `developer_id` (PK), `nickname`, `email`, `country`

**2. projects** — проекты
- `project_id` (PK), `name`, `repo_url`, `is_active`

**3. languages** — языки программирования
- `language_id` (PK), `name`

**4. editors** — редакторы/IDE
- `editor_id` (PK), `name`, `version`

**5. activity_sessions** — сессии активности (главная таблица)
- `activity_id` (PK)
- `developer_id` (FK → developers)
- `project_id` (FK → projects)
- `language_id` (FK → languages)
- `editor_id` (FK → editors)
- `start_time`, `duration_minutes`, `branch`, `os`, `is_remote`, `notes`

**6. daily_metrics** — дневные метрики
- `metric_id` (PK)
- `developer_id` (FK → developers)
- `day`, `coding_minutes`, `keystrokes`, `insertions`, `deletions`

---

## 🔧 Требования

- **PostgreSQL** (рекомендуется версия 14+)
- **Клиентская библиотека libpq** (заголовки и библиотека для компиляции)
- **CMake** 3.10+
- **Компилятор C++17** (gcc, clang или MSVC)

### Установка PostgreSQL (macOS)
```bash
brew install postgresql@14
brew services start postgresql@14
```

### Установка PostgreSQL (Linux/Ubuntu)
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib libpq-dev
sudo systemctl start postgresql
```

---

## 🚀 Быстрый старт

### 1. Создание и инициализация БД
```bash
# Создать базу данных
createdb lab6

# Залить схему и тестовые данные
psql -d lab6 -f sql/init.sql
```

### 2. Сборка приложения
```bash
# Удалить старую сборку (если была)
rm -rf build

# Конфигурация и сборка
cmake -S . -B build
cmake --build build
```

### 3. Запуск
```bash
./build/lab6
```

По умолчанию используется строка подключения: `dbname=lab6 host=localhost`

Можно указать свою строку подключения:
```bash
./build/lab6 "dbname=lab6 user=myuser password=mypass host=localhost"
```

---

## 📊 Функциональность (10 типов запросов)

Интерактивное меню предоставляет 10 различных запросов, покрывающих требования ТЗ:

### 1. Проекты и общее время
- `SELECT` + `LEFT JOIN` + `SUM` + `GROUP BY`
- Показывает все проекты и суммарное время работы по каждому

### 2. Последние 10 сессий (JOIN)
- Комплексный `JOIN` по 5 таблицам (developers, projects, languages, editors, activity_sessions)
- Форматирование даты через `to_char()`

### 3. Сессии по языку (WHERE)
- Фильтрация по языку программирования и минимальной длительности
- Использование `WHERE` для двух условий

### 4. Время по проектам (AGGREGATE)
- Группировка с агрегатными функциями `COUNT()` и `SUM()`
- Сортировка по убыванию времени

### 5. Средняя длительность по разработчику (AVG)
- Агрегатная функция `AVG()` с округлением `ROUND()`
- Фильтрация по конкретному разработчику

### 6. Топ языков по времени (LIMIT)
- `GROUP BY` + `SUM()` + `ORDER BY` + `LIMIT`
- Показывает топ-5 языков по времени использования

### 7. Использование редакторов (WINDOW/HAVING)
- `WITH` (CTE) + оконные функции `OVER()`
- Вычисление процентной доли каждого редактора

### 8. Нагруженные дни (HAVING)
- `JOIN` + `GROUP BY` + `HAVING`
- Поиск дней с нагрузкой >= N минут

### 9. Лидерборд проектов разработчика (SUBQUERY)
- Подзапрос в `WHERE` для фильтрации по developer_id
- Группировка результатов по проектам

### 10. Добавить новую сессию (INSERT)
- `INSERT` с подзапросами для получения FK
- Демонстрация модификации данных

---

## 📝 Тестовые данные

После выполнения `psql -d lab6 -f sql/init.sql` в БД будут:
- 5 разработчиков (alice, bob, carol, dave, eve)
- 5 проектов (ChronoTrack, DeepSearch, PixelWave, InfraTools, LegacyMonolith)
- 6 языков (Python, Go, C++, TypeScript, Rust, SQL)
- 5 редакторов (VS Code, CLion, Neovim, IntelliJ IDEA, Vim)
- 12 сессий активности
- 9 записей дневных метрик

Это позволяет сразу протестировать все запросы с осмысленными результатами.

---

## ✅ Результаты тестирования

Все 10 запросов успешно протестированы:

```
✓ Запрос 1: Проекты и общее время (5 проектов)
✓ Запрос 2: Последние 10 сессий (10 записей с JOIN)
✓ Запрос 4: Время по проектам (агрегация)
✓ Запрос 5: Средняя длительность alice = 78.8 мин
✓ Запрос 6: Топ-5 языков (Rust, Go, TypeScript, Python, C++)
✓ Запрос 7: Доля редакторов (VS Code 51.3%, CLion 30.8%...)
✓ Запрос 8: Нагруженные дни >= 100 мин (7 записей)
✓ Запрос 9: Лидерборд alice (3 проекта)
✓ Запрос 10: INSERT новой сессии (успешно добавлена)
```

---

## 🐳 Docker (опционально)

Если хотите запустить PostgreSQL в контейнере:

```bash
# Запуск PostgreSQL + Adminer (веб-админка)
docker compose up -d

# Adminer будет доступен на http://localhost:8080
# Server: db
# User: lab6
# Password: lab6
# Database: lab6
```

Для остановки:
```bash
docker compose down
```

---

## 🛡️ Безопасность

**Важно:** В данной лабораторной работе для простоты используется конкатенация строк при формировании SQL-запросов. В реальных приложениях **обязательно** используйте параметризованные запросы (`PQexecParams`) для защиты от SQL-инъекций.

---

## 📚 Соответствие требованиям ТЗ

✅ **БД в PostgreSQL** — реализовано  
✅ **Минимум 5 таблиц** — 6 таблиц с явными внешними ключами  
✅ **Интеграция с C++** — через libpq  
✅ **10 различных запросов** — реализовано  
✅ **JOIN и вариации** — LEFT JOIN, множественные JOIN  
✅ **WHERE/HAVING** — есть в запросах 3, 8  
✅ **Агрегатные функции** — SUM, COUNT, AVG, ROUND  
✅ **Сложные запросы** — CTE, оконные функции, подзапросы  
✅ **Тестовые данные** — полный набор для демонстрации

---

## 📧 Контакты

При возникновении вопросов обращайтесь к преподавателю или консультируйтесь с [официальной документацией PostgreSQL](https://www.postgresql.org/docs/).
